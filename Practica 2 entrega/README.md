## Uso y Despliegue con Docker

Esta aplicación está empaquetada en una imagen Docker para facilitar su despliegue.

### a. Cómo Construir la Imagen

Para construir la imagen localmente, ejecuta:

```bash
docker build -t pelayo/devops-practica:latest .
